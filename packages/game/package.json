{
  "name": "@fork48/game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && esbuild index.ts --bundle --minify --platform=node --outfile=index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "thatretrodev",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.16",
    "esbuild": "^0.14.20",
    "typescript": "^4.5.5"
  }
}
